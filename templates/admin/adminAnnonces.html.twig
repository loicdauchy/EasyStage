{% extends 'base.html.twig' %}

{% block title %}Admin - Edite - Users{% endblock %}

{% block body %}
{% if not app.user %} 

    <h2>Vous n'avez pas les droits</h2>
    <a href=" {{ path('security_login') }} "><button class="btn btn-primary">Connexion</button></a>

{% else %}
<div class="container cent">
    <div class="row cent justify-content-center align-items-center">
        <div class="col-12">
            <h2>Modérer l'annonce: <span class="text-info">{{annonce.titre}}</span></h2>
            {{ form_start(annonceForm) }}
                <div class="form-group">
                    {{ form_row(annonceForm.titre, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(annonceForm.contenu, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(annonceForm.specialites, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_row(annonceForm.status, {'attr': {'class': 'form-control'}}) }}
                </div>
                <button type="submit" class="btn btn-success">Publier</button>
            {{ form_end(annonceForm) }}

            
        </div>
    </div>
</div>
{% endif %}
{% endblock %}

